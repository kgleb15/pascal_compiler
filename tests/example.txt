Program Test;
x = 42;
if x = 42 then
begin
    y := x + 1;
    x := x - 1;
end
else
begin
    y := 0;
end;

a := 255;
b := $FF;
c := &17;
d := %1111;
e := 3.14;
f := 5E2;
g := 5.2E-3;


// однострочный комментарий
h := 1;

{ комментарий в фигурных скобках }
i := 2;

(* комментарий в круглых скобках со звёздочкой *)
j := 3;

{ 123
  многострочный комментарий
  на несколько строк
}
k := 4;

{ comment 1 (* comment 2 *) }
(* comment 1 { comment 2 } *)
{ comment 1 } comment 2 }
(* comment 1 ( * comment 2 *) comment 3 *)
l := 5;

(* comment 1 (* comment 2 *) *)
m := 6;

{ comment 1 { comment 2 } }
n := 7;

o := 8; // comment (* nested *)

// строки: базовые случаи
q := 'Hello, world!';
r := '';
s := 'a';

// экранирование кавычки
t := 'it''s a test';
u := '''';
v := '''quoted''';

// control string
w := #9;
x := #13#10;
y := 'Tab:'#9'End';
z := 'Line1'#13#10'Line2';
zz := 'a'#62'b';
pp := #$41;
rr := #&101;
ss := #%1000001;
// с пробелами между частями
zzz := 'a' #62 'b';

// caret-нотация
kk := ^Z;
kkk := ^ZZ // не caret-нотация

// строка, содержащая символы похожие на другие лексемы
aa := 'contains // not a comment';
bb := 'contains { not a comment } too';
cc := 'contains (* not a comment *) too';
dd := '42 and 3.14 inside a string';

ff := #300;
gg := #abc; // ошибка компиляции TODO: правильно обработать

// Строка содержит \n
q := 'abc
// незакрытая строка (конец файла)
qq := '''
